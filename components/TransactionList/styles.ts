import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 16,
    marginTop: 5,
  },
  titleContainer: {
    marginBottom: 5,
  },
  title: {
    fontSize: 21,
    fontStyle: "italic",
  },
});
