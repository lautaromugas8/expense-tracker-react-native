import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    display: "flex",
    alignItems: "center",
    marginBottom: 30,
  },
  title: {},
  balance: {
    fontSize: 32,
    fontWeight: "600",
  },
});
